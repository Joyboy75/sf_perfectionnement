{% extends "front/base.html.twig" %}

{% block main %}

{# <img src="{{asset('media/' ~ writer.media)}}" alt="{{writer.name}}"> #}

	<h2>{{ writer.name }}</h2>
	<p>{{ writer.firstname }}</p>


{% endblock %}